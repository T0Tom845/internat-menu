
spring:
  application:
    name: internat-menu
  datasource:
    url: jdbc:postgresql://localhost:5433/internat-menu
    username: internat
    password: internat
  flyway:
    schemas:
      - public
      - catalogue
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
server:
  port: 8081
